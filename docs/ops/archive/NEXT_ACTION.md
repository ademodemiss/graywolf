# Next Action
1. `monitor/learning_recovery_coordinator.py`'i `PYTHONPATH=/home/adem/graywolf` ve `--telegram` ile çalıştır; summary çıktısını, `learning_recovery_suggestions.json`’u ve `logs/learning_recovery_summary.log`’daki son entry’yi kontrol et.
2. Cron job `Phase 269 Learning Recovery Coordinator`’ın `openclaw cron list`’te aktif ve `nextRunAt` güncel olduğundan emin ol. Script’in her 4 saatte bir `logs/learning_recovery_summary.log`’a entry yazdığından ve Telegram’da raporlandığından emin ol.
3. Phase 270 planını uygulamak için `monitor/learning_recovery_inspector.py`’ı çalıştır; sample loglar ile `logs/learning_recovery_insights.json` çıktılarını test et, `learning_recovery_runbook.md`’i ops’a anlat, dashboard kartlarını yeni insights’la güncelle ve `dashboard/server.py --test` ile `learning_recovery_insights` alanının beklendiği gibi geldiğini doğrula.
4. Cron job `Phase 270 Learning Recovery Inspector`’ın `openclaw cron list`’te aktif, `nextRunAt` 6 saatlik aralıklarla ilerliyor ve Telegram raporlarıyla `logs/learning_recovery_insights.json`’u otomatik güncellediğinden emin ol.
5. `planning/phase271_plan.md` ve `docs/phase271_monitoring.md` Phase 271 hedeflerini yönlendiriyor; Phase 271 Reliability Alert akışını hazırlarken hangi metriklerin yeterli olduğuna karar ver ve runbook ile dashboard dokümanlarını güncel tut.
6. Reliability alarm script’iyle (`monitor/learning_recovery_alerts.py --telegram`) `logs/learning_recovery_alerts.log`’a entry üret; severity warning/critical durumlarında Telegram mesajı atılıyor mu kontrol et ve log’ta `learning_flag`/`processed_flag` gör.
7. Cron job `Phase 271 Reliability Alert`’in `openclaw cron list`’te aktif, `nextRunAt` 12 saatlik aralıklarla ilerliyor ve son çalışmaları `logs/learning_recovery_alerts.log`’a yeni satırlar yazıyor; bu job gerektiğinde ops’a reliability summary yolluyor mu doğrula.
8. `monitor/learning_recovery_trend.py`'i `PYTHONPATH=/home/adem/graywolf` ile çalıştır; önce `--dry-run` ile çıktıyı kontrol et, sonra (gerekirse) doğrudan komutu çalıştırarak `logs/learning_recovery_trend.log`’a entry yazdır; severity/reasons ve `latest_feedback` bilgileri doğru mu bak.
9. `Phase 272 Trend Watcher` cron job’unun `openclaw cron list`’te aktif, `nextRunAt` 6 saatlik aralıklarla ilerliyor ve Telegram’da warning/critical summary’leri gönderiyor olmasına dikkat et, `logs/learning_recovery_trend.log`’ta yeni satır/ts, `dashboard/templates/index.html`’de “Trend Metrikleri” kartının güncel olduğunu `/api/status` çıktısıyla doğrula.
10. Planlandığı gibi önce `monitor` modülünü düzeltip `PYTHONPATH=/home/adem/graywolf /home/adem/graywolf monitor/replan_health_reporter.py --telegram` komutunu çalıştırarak Phase 265 replan sağlık raporunu al; çıktı ile `logs/learning_recovery.log`/`learning_recovery_alerts.log` girdilerini ve varsa Telegram notlarını kontrol ettikten sonra Phase 272 trend/pending zincirine ve dokümantasyon/cron doğrulamalarına geç.